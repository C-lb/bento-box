import { SpliceClient } from "./SpliceClient";

export default function SplicePage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Trim and join clips</h1>
      <SpliceClient />
    </div>
  );
}
