import { ResizeClient } from "./ResizeClient";

export default function ResizePage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Compress or resize images</h1>
      <ResizeClient />
    </div>
  );
}
